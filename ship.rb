class Ship
  def initialize(length)
    @length = length
  end
  def length
    @length
  end
  def place(across_start,down_start,orientation)
    @across_start = across
    @down_start = down
    @orientation = orientation
  end

  def
end

ship = Ship.new(4)
